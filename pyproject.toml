[project]
name = "html2mu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "html-to-markdown>=1.9.0",
    "lxml>=6.0.1",
    "mistune>=3.1.3",
    "requests>=2.32.5",
]
[tool.uv]
package = true
